VITE_API_URL=http://localhost:8001/api

# Base URL where the separate Employee Portal (the `employee/` app) is hosted.
# Used only to build the "Employee Portal Access" login link shown to
# SuperAdmin in Employee Management (Copy Login Link / WhatsApp / Email).
VITE_EMPLOYEE_PORTAL_URL=http://localhost:3002
